name: Crawl Hot (D+0~3)

on:
  schedule:
    # Every 2 hours, 06-22 KST (UTC: 21-13, spans two days)
    # KST = UTC+9, so 06 KST = 21 UTC (prev day), 22 KST = 13 UTC
    - cron: '0 21,0,3,6,9,12 * * *'
  workflow_dispatch:

jobs:
  crawl:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        pip install -r requirements.txt

    - name: Create Service Account Key
      shell: python
      env:
        GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
      run: |
        import os
        import base64
        
        secret_val = os.environ.get("GCP_SA_KEY", "")
        if not secret_val:
            print("ERROR: Secret is empty!")
            exit(1)
            
        try:
            decoded = base64.b64decode(secret_val.strip()).decode('utf-8')
            content = decoded
        except Exception:
            content = secret_val

        import json
        try:
            json.loads(content)
        except Exception as e:
            print(f"ERROR: Content is NOT valid JSON: {e}")
            exit(1)

        with open("service-account.json", "w") as f:
            f.write(content)

    - name: Run Crawler (D+0 to D+3)
      env:
        PROJECT_ID: "golf-ai-480805"
        CRAWL_START_DAY: "0"
        CRAWL_END_DAY: "3"
        TEESCAN_PROXY_URL: ${{ secrets.TEESCAN_PROXY_URL }}
      run: |
        python ingest_data.py
